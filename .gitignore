# Secrets — never commit
.env.local
.env*.local
.env

# Allow the template
!.env.example

# Node / Next.js
node_modules/
.next/

# Supabase CLI local state (keep migrations/, config.toml, seed.sql)
supabase/.branches/
supabase/.temp/
supabase/.env
